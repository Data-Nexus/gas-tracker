{
  "name": "gas-tracker",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gas-tracker",
    "create-local": "graph create --node http://localhost:8020/ gas-tracker",
    "remove-local": "graph remove --node http://localhost:8020/ gas-tracker",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gas-tracker"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
